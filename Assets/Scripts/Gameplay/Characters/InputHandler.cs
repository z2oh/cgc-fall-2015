using UnityEngine;
using System.Collections;

public class InputHandler : MonoBehaviour
{
    // This is just a helper class that is used to determine if a character should accept input.
    public bool shouldUpdate;
}
